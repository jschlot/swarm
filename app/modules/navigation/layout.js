import React, { PropTypes } from 'react';

import { navigation } from './styles.scss';

const Navigation = () => {
    return (
        <nav className="navigation">
            <div className="navigation__title">
                SWARM
            </div>
        </nav>
    );
};

Navigation.propTypes = {
};

export default Navigation;
